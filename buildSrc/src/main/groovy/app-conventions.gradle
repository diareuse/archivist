plugins {
    id "application"
    id "common-conventions"
    id "com.github.johnrengelman.shadow"
}

shadowJar {
    minimize {
        exclude(dependency("org.slf4j:.*:.*"))
    }
    mergeServiceFiles()
}
